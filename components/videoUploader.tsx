'use client'

export default function VideoUploader(){
    async function handleSubmit() {
        
    }
    return (
        <div>
         <input type="file"  />
         <button onClick={handleSubmit}></button>
        </div>
    )
}